# Copy to .env (gitignored) for local runs, or export these in your shell.
#
# Path to the Google service-account JSON key file. The key itself is provided
# at runtime only (mounted read-only in the container) and is NEVER committed.
GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/service-account.json
